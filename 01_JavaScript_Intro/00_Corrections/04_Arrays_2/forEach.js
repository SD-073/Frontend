// Initialize an Array of Numbers:
// Create an array called numbers containing the values in order: 1, 2, 3, 4, 5.
const numbers = [1, 2, 3, 4, 5];
const students = ['Alex', 'Ahmed', 'Claus', 'Grace', 'Kai'];

// Use the forEach method to iterate over numbers and print each number to the console.
// students.forEach(function (a, b) {
//   console.log(a, b);
// });

// ES6
// numbers.forEach((el) => console.log(el));
// students.forEach((el, i) => console.log(el, i));

// Use the forEach Method to Calculate the Sum of the Numbers:
// Initialize a variable sum to 0.

let sum = 0;
// Use the forEach method to iterate over numbers, adding each number to sum.
// numbers.forEach((number) => (sum = sum + number));
// numbers.forEach((number) => (sum += number));
// Print the final value of sum to the console.
// console.log(sum);

// Use the forEach Method to Create a New Array with Squared Values:
// Create an empty array called squaredNumbers.
// const squaredNumbers = [];
// numbers.forEach((number) => squaredNumbers.push(number * number));
// console.log(squaredNumbers);

// const squaredNumbersMap = numbers.map((number) => number * number);
// console.log(squaredNumbersMap);
